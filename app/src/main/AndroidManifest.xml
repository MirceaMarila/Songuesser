<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.songuesser">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Songuesser">
        <activity android:name="game.ProfileActivity"></activity>
        <activity android:name="game.LeaderboardActivity" />
        <activity android:name="game.Round5Activity" />
        <activity android:name="game.Round4Activity" />
        <activity android:name="game.Round3Activity" />
        <activity android:name="game.Round2Activity" />
        <activity android:name="game.Round1Activity" />
        <activity android:name="game.PlayActivity" />
        <activity android:name=".main_menu.LoginActivity" />
        <activity android:name=".main_menu.RegisterActivity" />
        <activity android:name=".main_menu.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>